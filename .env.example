# Notification settings (optional)
NOTIFICATION_SERVICE = "apprise" # Options: "apprise", "discord" Omit this line to disable notifications
WEBHOOK_URL = "https://apprise.example.com/notify/apprise" # Omit this line will also disable notifications
APPRISE_TAG = "zfs" # Only for apprise notifications